extends ./config/base
include mixins/dailylogMixins

block scripts
    script(src="/static/js/dailyLog.js", defer)

block content   
    div 
        ul 
            each log in dailyLogs 
                +dailyLog(log)         
            else 
                li 아직 아무것도 없습니다.        
    div
        button.add_dailyLog 추가
        button.edit_dailyLog 편집 
